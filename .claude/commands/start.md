Read the file `ROADMAP.md` in the current project directory. Then check if `.claude/session-log.md` exists — if it does, read it too.

Based on what you read, do the following:

1. Show a short summary of the project: what it is, the stack, and the API base URL.
2. Show which phases are fully done (all checkboxes checked), which are in-progress, and which haven't started yet.
3. If a session log exists, show the "Last session summary" section so the user knows exactly where they left off and what the next step is.
4. State clearly: **"Next task:"** — the very first unchecked item in the roadmap, with its phase name.
5. Ask the user if they want to continue with that next task or jump to something specific.

Keep the briefing concise — no more than 20 lines total. Use plain text, no heavy formatting.
